chiffres = [1, 3, 5, 7, 9, 11]
somme = sum(chiffres)
nombre_element = len(chiffres)

moyenne = somme/nombre_element

print("la moyenne est: ", moyenne)

note_basse = min(chiffres)
print("la note basse est :",note_basse)
note_haute = max(chiffres)
print("la note haute est: ", note_haute)
notes_total = len(chiffres)
print("la note total est: ", notes_total)